# Αναβάθμιση του PIP
python.exe -m pip install --upgrade pip

# Εγκατάσταση του πακέτου PyInstaller για το Compile
pip install pyinstaller

# Χρήση του πακέτου sys, το οποίο είναι τμήμα Standard Python Library
# Provides access to some variables used or maintained by the interpreter and to functions that interact strongly with the interpreter

# Χρήση του πακέτου os, το οποίο είναι τμήμα Standard Python Library
# Provides a way of using operating system-dependent functionality like reading or writing to the file system

# Χρήση του πακέτου threading, το οποίο είναι τμήμα Standard Python Library
# Used to run multiple threads (tasks) at once

# Χρήση του πακέτου time, το οποίο είναι τμήμα Standard Python Library
# Provides various time-related functions

# Εγκατάσταση του πακέτου Paramiko
# Used for SSH connections
pip install paramiko

# Έγκατάσταση του πακέτου TermColor
# Used for colored terminal text output
pip install termcolor

# Εγκατάσταση του πακέτου IPy
pip install IPy